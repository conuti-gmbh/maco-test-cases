{
          "stammdaten": {
            "MARKTLOKATION": [
        {
          "marktlokationsId": "${data:jsonPath('$.marktlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.marktlokationsId')})}",
          "zaehlwerke": [
            {
              "obisKennzahl": "${data:jsonPath('$.obisKennzahl'):isEmpty():ifElse('null',${data:jsonPath('$.obisKennzahl')})}",
              "zaehlzeiten": {
                "zaehlzeitDefinition": "${data:jsonPath('$.zaehlzeitDefinition'):isEmpty():ifElse('null',${data:jsonPath('$.zaehlzeitDefinition')})}",
                "register": "${data:jsonPath('$.register'):isEmpty():ifElse('null',${data:jsonPath('$.register')})}"
              },
              "wertegranularitaet": "${data:jsonPath('$.wertegranularitaet'):isEmpty():ifElse('null',${data:jsonPath('$.wertegranularitaet')})}"
            },
            {
              "obisKennzahl": "${data:jsonPath('$.obisKennzahl1'):isEmpty():ifElse('null',${data:jsonPath('$.obisKennzahl1')})}",
              "zaehlzeiten": {
                "zaehlzeitDefinition": "${data:jsonPath('$.zaehlzeitDefinition1'):isEmpty():ifElse('null',${data:jsonPath('$.zaehlzeitDefinition1')})}",
                "register": "${data:jsonPath('$.register1'):isEmpty():ifElse('null',${data:jsonPath('$.register1')})}"
              },
              "wertegranularitaet": "${data:jsonPath('$.wertegranularitaet1'):isEmpty():ifElse('null',${data:jsonPath('$.wertegranularitaet1')})}"
            }
          ]
        }
      ],
      "MESSLOKATION": [
        {
          "messlokationsId": "${data:jsonPath('$.messlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.messlokationsId')})}"
        }
      ],
      "NETZNUTZUNGSVERTRAG": [
        {
          "vertragsart": "${data:jsonPath('$.vertragsart'):isEmpty():ifElse('null',${data:jsonPath('$.vertragsart')})}",
          "lokationsId": "${data:jsonPath('$.lokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.lokationsId')})}",
          "lokationsTyp": "${data:jsonPath('$.lokationsTyp'):isEmpty():ifElse('null',${data:jsonPath('$.lokationsTyp')})}"
        }
      ],
      "ZAEHLER": [
        {
          "zaehlernummer": "${data:jsonPath('$.zaehlernummer'):isEmpty():ifElse('null',${data:jsonPath('$.zaehlernummer')})}",
          "messlokationsId": "${data:jsonPath('$.messlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.messlokationsId')})}",
          "gateway": "${data:jsonPath('$.gateway'):isEmpty():ifElse('null',${data:jsonPath('$.gateway')})}"
        }
      ],
      "BILANZIERUNG": [
        {
          "marktlokationsId": "${data:jsonPath('$.marktlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.marktlokationsId')})}"
        }
      ]
    },
    "transaktionsdaten": {
      "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
      "absender":"${data:jsonPath('$.absender'):isEmpty():ifElse('null',${data:jsonPath('$.absender')})}",
      "empfaenger":"${data:jsonPath('$.empfaenger'):isEmpty():ifElse('null',${data:jsonPath('$.empfaenger')})}",
      "kategorie": "${data:jsonPath('$.kategorie'):isEmpty():ifElse('null',${data:jsonPath('$.kategorie')})}",
      "gueltigAb": "${data:jsonPath('$.gueltigAb'):isEmpty():ifElse('null',${data:jsonPath('$.gueltigAb')})}",
      "nachrichtenReferenzBestellbestaetigung": "${data:jsonPath('$.nachrichtenReferenzBestellbestaetigung'):isEmpty():ifElse('null',${data:jsonPath('$.nachrichtenReferenzBestellbestaetigung')})}",
      "vorgangsReferenzBestellbestaetigung": "${data:jsonPath('$.vorgangsReferenzBestellbestaetigung'):isEmpty():ifElse('null',${data:jsonPath('$.vorgangsReferenzBestellbestaetigung')})}"
          }
}