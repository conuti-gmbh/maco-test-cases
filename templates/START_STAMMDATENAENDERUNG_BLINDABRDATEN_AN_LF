{
  "stammdaten": {
  "VERWENDUNGSZEITRAUM": [
        {
          "zeitraumId": ${data:jsonPath('$.zeitraumId'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId')})},
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet')})}",
          "verwendungAb": "${data:jsonPath('$.verwendungAb'):isEmpty():ifElse('null',${data:jsonPath('$.verwendungAb')})}",
          "verwendungBis": "${data:jsonPath('$.verwendungBis'):isEmpty():ifElse('null',${data:jsonPath('$.verwendungBis')})}"
        },
        {
          "zeitraumId": ${data:jsonPath('$.zeitraumId2'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId2')})},
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet2'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet2')})}",
          "verwendungAb": "${data:jsonPath('$.verwendungAb2'):isEmpty():ifElse('null',${data:jsonPath('$.verwendungAb2')})}"
        }
      ],
      "NETZLOKATION": [
        {
          "netzlokationsId": "${data:jsonPath('$.netzlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.netzlokationsId')})}",
          "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
          "zeitraumId": ${data:jsonPath('$.zeitraumId'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId')})},
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet')})}",
          "abrechnungsdaten": [
            {
              "artikelId": "${data:jsonPath('$.artikelId'):isEmpty():ifElse('null',${data:jsonPath('$.artikelId')})}",
              "artikelIdTyp": "${data:jsonPath('$.artikelIdTyp'):isEmpty():ifElse('null',${data:jsonPath('$.artikelIdTyp')})}",
              "abrechnungBlindarbeit": ${data:jsonPath('$.abrechnungBlindarbeit'):isEmpty():ifElse('null',${data:jsonPath('$.abrechnungBlindarbeit')})},
              "zahlerBlindarbeit": "${data:jsonPath('$.zahlerBlindarbeit'):isEmpty():ifElse('null',${data:jsonPath('$.zahlerBlindarbeit')})}"
            }
          ]
        },
        {
          "netzlokationsId": "${data:jsonPath('$.netzlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.netzlokationsId')})}",
          "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
          "zeitraumId": ${data:jsonPath('$.zeitraumId2'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId2')})},
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet2'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet2')})}",
          "abrechnungsdaten": [
            {
              "artikelId": "${data:jsonPath('$.artikelId2'):isEmpty():ifElse('null',${data:jsonPath('$.artikelId2')})}",
              "artikelIdTyp": "${data:jsonPath('$.artikelIdTyp2'):isEmpty():ifElse('null',${data:jsonPath('$.artikelIdTyp2')})}",
              "abrechnungBlindarbeit": ${data:jsonPath('$.abrechnungBlindarbeit2'):isEmpty():ifElse('null',${data:jsonPath('$.abrechnungBlindarbeit2')})}
            }
          ]
        }
      ]
    },
  "transaktionsdaten": {
    "empfaenger": "${data:jsonPath('$.empfaenger')}",
    "absender": "${data:jsonPath('$.absender')}",
    "sparte": "${data:jsonPath('$.sparte')}",
    "kategorie": "${data:jsonPath('$.kategorie')}"
  }
  }
 