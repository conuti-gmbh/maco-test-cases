{
    "stammdaten": {
      "VERWENDUNGSZEITRAUM": [
        {
          "zeitraumId": ${data:jsonPath('$.zeitraumId'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId')})},
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet')})}",
          "verwendungAb": "${data:jsonPath('$.verwendungAb'):isEmpty():ifElse('null',${data:jsonPath('$.verwendungAb')})}",
          "verwendungBis": "${data:jsonPath('$.verwendungBis'):isEmpty():ifElse('null',${data:jsonPath('$.verwendungBis')})}"
        }
      ],
      "MESSLOKATION": [
        {
          "messlokationsId": "${data:jsonPath('$.messlokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.messlokationsId')})}",
          "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
          "gueltigkeitszeitraum": {
            "zeitraumId": ${data:jsonPath('$.zeitraumId'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId')})},
            "startdatum": "${data:jsonPath('$.startdatum'):isEmpty():ifElse('null',${data:jsonPath('$.startdatum')})}"
          },
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet')})}",
          "marktrollen": [
            {
              "boTyp": "MARKTTEILNEHMER",
              "versionStruktur": "1",
              "marktrolle": "${data:jsonPath('$.marktrolle1'):isEmpty():ifElse('null',${data:jsonPath('$.marktrolle1')})}",
              "gewerbekennzeichnung": true,
              "rollencodenummer": "${data:jsonPath('$.rollencodenummer1'):isEmpty():ifElse('null',${data:jsonPath('$.rollencodenummer1')})}",
              "rollencodetyp": "BDEW",
              "weiterverpflichtet": ${data:jsonPath('$.weiterverpflichtet'):isEmpty():ifElse('null',${data:jsonPath('$.weiterverpflichtet')})},
              "messstellenbetreiberEigenschaft": "${data:jsonPath('$.messstellenbetreiberEigenschaft'):isEmpty():ifElse('null',${data:jsonPath('$.messstellenbetreiberEigenschaft')})}"
            },
            {
              "boTyp": "MARKTTEILNEHMER",
              "versionStruktur": "1",
              "marktrolle": "${data:jsonPath('$.marktrolle2'):isEmpty():ifElse('null',${data:jsonPath('$.marktrolle2')})}",
              "gewerbekennzeichnung": true,
              "rollencodenummer": "${data:jsonPath('$.rollencodenummer2'):isEmpty():ifElse('null',${data:jsonPath('$.rollencodenummer2')})}",
              "rollencodetyp": "BDEW"
            }
          ]
          "netzebenemessung": "${data:jsonPath('$.netzebenemessung'):isEmpty():ifElse('null',${data:jsonPath('$.netzebenemessung')})}"
        }
      ],
      "MESSSTELLENBETRIEBSVERTRAG": [
        {
          "vertragsart": "MESSSTELLENBETRIEBSVERTRAG",
          "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
          "gueltigkeitszeitraum": {
            "zeitraumId": ${data:jsonPath('$.zeitraumId'):isEmpty():ifElse('null',${data:jsonPath('$.zeitraumId')})},
            "startdatum": "${data:jsonPath('$.startdatum'):isEmpty():ifElse('null',${data:jsonPath('$.startdatum')})},"
          }
          "datenqualitaet": "${data:jsonPath('$.datenqualitaet'):isEmpty():ifElse('null',${data:jsonPath('$.datenqualitaet')})}",
          "vertragskonditionen": {
            "abrechnungUeberNna": "${data:jsonPath('$.abrechnungUeberNna'):isEmpty():ifElse('null',${data:jsonPath('$.abrechnungUeberNna')})}"
          },
          "lokationsId": "${data:jsonPath('$.lokationsId'):isEmpty():ifElse('null',${data:jsonPath('$.lokationsId')})}",
          "lokationsTyp": "${data:jsonPath('$.lokationsTyp'):isEmpty():ifElse('null',${data:jsonPath('$.lokationsTyp')})}"
        }
      ]
    },
    "transaktionsdaten": {
      "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
      "absender": {
        "rollencodenummer": "${data:jsonPath('$.absender'):isEmpty():ifElse('null',${data:jsonPath('$.absender')})}"
      },
      "empfaenger": {
        "rollencodenummer": "${data:jsonPath('$.empfaenger'):isEmpty():ifElse('null',${data:jsonPath('$.empfaenger')})}"
      },
      "kategorie": "Z88"
    }
}