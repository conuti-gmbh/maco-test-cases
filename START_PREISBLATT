    
    {
      "stammdaten": {
      "PREISBLATT": [
        {
          "nichtGenutzt": ${data:jsonPath('$.nichtGenutzt'):isEmpty():ifElse('null',${data:jsonPath('$.nichtGenutzt')})},
          "gueltigkeit": {
            "startdatum": "${data:jsonPath('$.startdatum'):isEmpty():ifElse('null',${data:jsonPath('$.startdatum')})}"
          },
          "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
          "preispositionen": [
            {
              "preiseinheit": "${data:jsonPath('$.preiseinheit'):isEmpty():ifElse('null',${data:jsonPath('$.preiseinheit')})}",
              "preisstaffeln": [
                {
                  "einheitspreis": ${data:jsonPath('$.einheitspreis'):isEmpty():ifElse('null',${data:jsonPath('$.einheitspreis')})}
                }
              ],
              "positionsnummer": ${data:jsonPath('$.positionsnummer'):isEmpty():ifElse('null',${data:jsonPath('$.positionsnummer')})},
              "artikelId": "${data:jsonPath('$.artikelId'):isEmpty():ifElse('null',${data:jsonPath('$.artikelId')})}"
            },
            {
              "preiseinheit": "${data:jsonPath('$.preiseinheit'):isEmpty():ifElse('null',${data:jsonPath('$.preiseinheit')})}",
              "preisstaffeln": [
                {
                  "einheitspreis": ${data:jsonPath('$.einheitspreis2'):isEmpty():ifElse('null',${data:jsonPath('$.einheitspreis2')})}
                }
              ],
              "positionsnummer": ${data:jsonPath('$.positionsnummer2'):isEmpty():ifElse('null',${data:jsonPath('$.positionsnummer2')})},
              "artikelId": "${data:jsonPath('$.artikelId2'):isEmpty():ifElse('null',${data:jsonPath('$.artikelId2')})}"
            }
          ]
        }
      ]
    },
    "transaktionsdaten": {
      "sparte": "${data:jsonPath('$.sparte'):isEmpty():ifElse('null',${data:jsonPath('$.sparte')})}",
      "absender": "${data:jsonPath('$.absender'):isEmpty():ifElse('null',${data:jsonPath('$.absender')})}",
      "empfaenger": "${data:jsonPath('$.empfaenger'):isEmpty():ifElse('null',${data:jsonPath('$.empfaenger')})}",
      "kategorie": "${data:jsonPath('$.kategorie'):isEmpty():ifElse('null',${data:jsonPath('$.kategorie')})}",
    }
    }